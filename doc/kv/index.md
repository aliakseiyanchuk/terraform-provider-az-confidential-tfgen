# `kv` group

This group contains the resources creating secrets, keys, and certificates in the Azure Key Vault service

```shell
tfgen kv help
```

The command above would print:
```
Usage: tfgen [<standard options>] kv <subcommand> [<args>]
Possible sub-commands are:
- secret
- key
- certificate
```

The res